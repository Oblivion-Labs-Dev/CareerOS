name: CI

on:
  push:
  pull_request:

defaults:
  run:
    working-directory: CareerOS

jobs:
  typecheck:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
        with:
          path: CareerOS
      - uses: pnpm/action-setup@v4
        with:
          version: 9
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm
          cache-dependency-path: CareerOS/pnpm-lock.yaml
      - run: pnpm install --no-frozen-lockfile
      - run: pnpm --filter @career-os/core build
      - run: pnpm --filter @career-os/ui build
      - run: pnpm typecheck

  extension-unit:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
        with:
          path: CareerOS
      - uses: pnpm/action-setup@v4
        with:
          version: 9
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm
          cache-dependency-path: CareerOS/pnpm-lock.yaml
      - run: pnpm install --no-frozen-lockfile
      - run: pnpm --filter @career-os/extension test

  api:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
        with:
          path: CareerOS
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install -r requirements.txt pytest httpx
        working-directory: CareerOS/apps/api
      # The real-browser tests drive Chromium through Playwright; the pip
      # package does not ship the browser itself.
      - run: python -m playwright install --with-deps chromium
        working-directory: CareerOS/apps/api
      - run: python -m compileall app
        working-directory: CareerOS/apps/api
      - run: pytest tests -q
        working-directory: CareerOS/apps/api

  web-e2e:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
        with:
          path: CareerOS
      - uses: pnpm/action-setup@v4
        with:
          version: 9
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm
          cache-dependency-path: CareerOS/pnpm-lock.yaml
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pnpm install --no-frozen-lockfile
      - run: pnpm --filter @career-os/core build
      - run: pnpm --filter @career-os/ui build
      - run: pnpm --filter @career-os/web build
      - run: pnpm --filter @career-os/web exec playwright install chromium --with-deps
      - run: pip install -r requirements.txt
        working-directory: CareerOS/apps/api
      - name: Start API and web for e2e
        run: |
          cd apps/api && python -m uvicorn app.main:app --host 127.0.0.1 --port 8000 &
          cd apps/web && pnpm start --port 3000 &
          npx --yes wait-on --timeout 180000 http://127.0.0.1:8000/health http://127.0.0.1:3000
      - run: pnpm --filter @career-os/web exec playwright test
        env:
          CI: "1"
