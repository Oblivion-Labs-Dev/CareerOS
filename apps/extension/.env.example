# AMO API credentials — copy to .env (never commit .env)
# Create at: https://addons.mozilla.org/developers/addon/api/key/

AMO_JWT_ISSUER=
AMO_JWT_SECRET=

# Optional: override AMO channel (listed | unlisted) when running publish:firefox
# AMO_CHANNEL=listed
