// The application cards share the dashboard's count-up so a figure animates the
// same way wherever it appears. Kept as a re-export rather than a second copy.
export { useCountUp } from "@/hooks/use-count-up";
